server {
        listen 80;
        server_name учивыучи.рф www.учивыучи.рф;

        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri /index.html;
  }
}